'use strict';

import hiker from './hiker.js';

describe('Should Style: Answer', () => {
  it('to life the universe and everything', () => {
    const hitch = hiker;
    ansewr().should.equal(42);
  });
});
